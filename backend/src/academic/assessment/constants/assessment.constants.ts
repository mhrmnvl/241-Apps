/**
 * What a teaching assignment's per-type weights must add up to.
 *
 * Percent, so the teacher reads the editor in the same units they would
 * describe the rule in: "harian 40, UTS 30, UAS 30".
 */
export const ASSESSMENT_WEIGHT_TOTAL = 100;
